# 作业 — 同题程序实现

**A 部分 — 两层网反向传播（平方损失）**：实现 `forward`/`backward` 并用差分校验。  
**B 部分 — 带重启/k++ 的 K-means**：实现并比较不同初始化与种子下的损失。  
**C 小练习 — L2 + 早停的验证集选择**：划分 train/val，网格搜索 \(\lambda\)，加早停并报告结果。

**评分**：正确性 60，工程 20，分析 20。
